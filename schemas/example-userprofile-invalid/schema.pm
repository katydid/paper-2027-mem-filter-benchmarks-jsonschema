#
# Generated by JSON Model Compiler version 2.0.50
# see https://github.com/clairey-zx81/json-model
#
use utf8;
use strict;
use warnings;
no warnings "uninitialized";
use re::engine::RE2;
package schema;
use Exporter 'import';
our @ISA = qw( Exporter );
our @EXPORT = qw(
    check_model_init
    check_model_mapper
    check_model
    check_model_free
);
use JSON::JsonModel;
use constant JMC_VERSION => "2.0.50";


sub json_model_1($$$);
my %check_model_map;

# check $ (.)
sub json_model_1($$$)
{
    my ($val, $path, $rep) = @_;
    # A representation of a user profile
    # .
    # check open must/may only props
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $pval;
    if (! exists $$val{"email"})
    {
        return 0;
    }
    $pval = $$val{"email"};
    # .email
    my $res = jm_is_string($pval) && jm_is_email($pval, undef, undef);
    if (! $res)
    {
        return 0;
    }
    if (! exists $$val{"username"})
    {
        return 0;
    }
    $pval = $$val{"username"};
    # .username
    $res = jm_is_string($pval);
    if (! $res)
    {
        return 0;
    }
    if (exists $$val{"age"})
    {
        $pval = $$val{"age"};
        # .age
        $res = jm_is_integer($pval) && $pval >= 0;
        if (! $res)
        {
            return 0;
        }
    }
    if (exists $$val{"fullName"})
    {
        $pval = $$val{"fullName"};
        # .fullName
        $res = jm_is_string($pval);
        if (! $res)
        {
            return 0;
        }
    }
    if (exists $$val{"interests"})
    {
        $pval = $$val{"interests"};
        # .interests
        $res = jm_is_array($pval);
        if ($res)
        {
            for my $arr_0_idx (0 .. $#$pval)
            {
                my $arr_0_item = $$pval[$arr_0_idx];
                # .interests.0
                $res = jm_is_string($arr_0_item);
                if (! $res)
                {
                    last;
                }
            }
        }
        if (! $res)
        {
            return 0;
        }
    }
    if (exists $$val{"location"})
    {
        $pval = $$val{"location"};
        # .location
        $res = jm_is_string($pval);
        if (! $res)
        {
            return 0;
        }
    }
    return 1;
}


sub jm_is_email($$$)
{
    my ($val, $path, $rep) = @_;
    my $res = $val =~ /^([-+!#\$%&'`*\/=?^{}|~_a-z0-9]+)(\.([-+!#\$%&'`*\/=?^{}|~_a-z0-9]+))*@([a-z0-9][-a-z0-9]{0,62})(\.([a-z0-9][-a-z0-9]{0,62}))*$/i;
    return $res;
}

# initialization of global variables

our $initialized = 0;

sub check_model_init()
{
    if (!$initialized)
    {
        $initialized = 1;
        %check_model_map = (
            "" => \&json_model_1,
        );
    }
}

sub check_model_free()
{
    if ($initialized)
    {
        $initialized = 0;
        %check_model_map = ();
    }
}

sub check_model_mapper($)
{
    my ($name) = @_;
    die "unexpected model name \"$name\"" unless exists $check_model_map{$name};
    return $check_model_map{$name};
}

sub check_model($$$)
{
    my ($json, $name, $rep) = @_;
    die "unexpected model name \"$name\"" unless exists $check_model_map{$name};
    my $path = defined $rep ? [] : undef;
    return $check_model_map{$name}($json, $path, $rep);
}

1;

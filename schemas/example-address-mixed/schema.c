//
// Generated by JSON Model Compiler version 2.0.50
// see https://github.com/clairey-zx81/json-model
//

#include <json-model.h>
#define JSON_MODEL_VERSION "2.0.50"

static bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_4(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[1];
const size_t check_model_map_size = 1;

// object .'&'.2.'|'.1.'^'.1
static INLINE bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
        return false;
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        if (jm_str_eq_8(prop, 0x6966664f74736f70LL) && jm_str_eq_6(prop + 8, 0x000000786f426563LL))
        {
            // handle must postOfficeBox property
            must_count += 1;
            // .'&'.2.'|'.1.'^'.1.postOfficeBox
            res = true;
            continue;
        }
        // accept any other props
    }
    return must_count == 1;
}

// object .'&'.2.'|'.0
static INLINE bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
        return false;
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        if (unlikely(jm_str_eq_8(prop, 0x6966664f74736f70LL) && jm_str_eq_6(prop + 8, 0x000000786f426563LL)))
        {
            // handle must postOfficeBox property
            must_count += 1;
            // .'&'.2.'|'.0.postOfficeBox
            res = true;
            continue;
        }
        else if (jm_str_eq_8(prop, 0x6441746565727473LL) && jm_str_eq_6(prop + 8, 0x0000007373657264LL))
        {
            // handle must streetAddress property
            must_count += 1;
            // .'&'.2.'|'.0.streetAddress
            res = true;
            continue;
        }
        // accept any other props
    }
    return must_count == 2;
}

// object .'&'.1.'|'.1.'^'.1
static INLINE bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
        return false;
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        if (jm_str_eq_8(prop, 0x6465646e65747865LL) && jm_str_eq_8(prop + 8, 0x0073736572646441LL))
        {
            // handle must extendedAddress property
            must_count += 1;
            // .'&'.1.'|'.1.'^'.1.extendedAddress
            res = true;
            continue;
        }
        // accept any other props
    }
    return must_count == 1;
}

// object .'&'.1.'|'.0
static INLINE bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
        return false;
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        if (unlikely(jm_str_eq_8(prop, 0x6465646e65747865LL) && jm_str_eq_8(prop + 8, 0x0073736572646441LL)))
        {
            // handle must extendedAddress property
            must_count += 1;
            // .'&'.1.'|'.0.extendedAddress
            res = true;
            continue;
        }
        else if (jm_str_eq_8(prop, 0x6441746565727473LL) && jm_str_eq_6(prop + 8, 0x0000007373657264LL))
        {
            // handle must streetAddress property
            must_count += 1;
            // .'&'.1.'|'.0.streetAddress
            res = true;
            continue;
        }
        // accept any other props
    }
    return must_count == 2;
}

// object .'&'.0
static INLINE bool _jm_obj_4(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
        return false;
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        if (unlikely(jm_str_eq_8(prop, 0x4e7972746e756f63LL) && jm_str_eq_4(prop + 8, 0x00656d61)))
        {
            // handle must countryName property
            must_count += 1;
            // .'&'.0.countryName
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x7974696c61636f6cLL) && jm_str_eq_0(prop + 8)))
        {
            // handle must locality property
            must_count += 1;
            // .'&'.0.locality
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        else if (unlikely(jm_str_eq_7(prop, 0x00006e6f69676572LL)))
        {
            // handle must region property
            must_count += 1;
            // .'&'.0.region
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        if (unlikely(jm_str_eq_8(prop, 0x6465646e65747865LL) && jm_str_eq_8(prop + 8, 0x0073736572646441LL)))
        {
            // handle may extendedAddress property
            // .'&'.0.extendedAddress
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x6966664f74736f70LL) && jm_str_eq_6(prop + 8, 0x000000786f426563LL)))
        {
            // handle may postOfficeBox property
            // .'&'.0.postOfficeBox
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x6f436c6174736f70LL) && jm_str_eq_3(prop + 8, 0x00006564)))
        {
            // handle may postalCode property
            // .'&'.0.postalCode
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x6441746565727473LL) && jm_str_eq_6(prop + 8, 0x0000007373657264LL)))
        {
            // handle may streetAddress property
            // .'&'.0.streetAddress
            res = json_is_string(pval);
            if (unlikely(! res))
                return false;
            continue;
        }
        // accept any other props
    }
    return must_count == 3;
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // An address similar to http://microformats.org/wiki/h-card
    // .
    // .'&'.0
    // .'&'.1
    // .'&'.1.'|'.0
    // .'&'.1.'|'.1
    // not-case xor list
    // .'&'.1.'|'.1.'^'.1
    // .'&'.2
    // .'&'.2.'|'.0
    // .'&'.2.'|'.1
    // not-case xor list
    // .'&'.2.'|'.1.'^'.1
    return _jm_obj_4(val, NULL, NULL) && (_jm_obj_3(val, NULL, NULL) || ! _jm_obj_2(val, NULL, NULL)) && (_jm_obj_1(val, NULL, NULL) || ! _jm_obj_0(val, NULL, NULL));
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 1);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        jm_sort_propmap(check_model_map_tab, 1);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
        initialized = false;

        // cleanup code
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
